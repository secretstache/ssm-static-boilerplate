{% macro render(props) %}

    <div class="grid-container">

        <div class="template-scroll-rotation__content">
            <h2>Scroll page to bottom</h2>

            <div class="template-scroll-rotation__cube-container">
                <div class="template-scroll-rotation__cube"  data-rotate-x="{{ props.rotateX }}" data-rotate-y="{{ props.rotateY }}">
                    {% for item in props.items %}
                        <div class="template-scroll-rotation__cube-face{% if item.itemClass %} {{item.itemClass}}{% endif%}">{{ item.itemTitle }}</div>
                    {% endfor %}
                </div>
            </div>
        </div>

    {% endmacro %}